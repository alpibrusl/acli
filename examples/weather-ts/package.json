{
  "name": "@acli-examples/weather",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ACLI weather CLI example (TypeScript) — mirrors examples/weather/weather.py",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "start": "node dist/weather.js"
  },
  "dependencies": {
    "@acli/sdk": "file:../../sdks/typescript"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
